using System;

namespace Security
{
    class Program
    {
        static void Main(string[] args)
        {
           
            CipherHandler cipherHandler = new CipherHandler();
            cipherHandler.Run();
        }
    }
}
